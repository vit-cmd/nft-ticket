type Event @entity {
  id: ID!
  name: String!
  location: String!
  description: String!
  hashImage: String!
  eventManager: Bytes! # address
  startDay: BigInt!
  endDay: BigInt!
}

type EventManager @entity {
  id: ID!
  approve: Boolean!
}
type NewTicketType @entity {
  id: ID!
  eventId: BigInt! # uint256
  name: String! # string
  description: String! # string
  hashImage: String! # string
  currentMintTickets: BigInt! # uint64
  maxTicketCount: BigInt! # uint64
  priceFactor: BigInt! # uint64
}
